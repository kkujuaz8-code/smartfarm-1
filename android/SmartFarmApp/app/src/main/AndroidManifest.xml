<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFarmApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity
            android:name=".activity.search.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.PumpSafetyService"
            android:stopWithTask="false" />
        <activity android:name=".activity.main.MainActivity" android:exported="false" />
        <activity android:name=".activity.main.GuestMainActivity" />
        <activity android:name=".activity.login.JoinActivity" android:exported="false" />

        <activity android:name=".activity.sensor.SensorActivity" />
        <activity android:name=".activity.search.PlantSearchActivity" />
        <activity android:name=".activity.search.PestSearchActivity" />

        <activity android:name=".activity.diary.DiaryActivity" />
        <activity android:name=".activity.diary.DiaryFolderActivity" />
        <activity android:name=".activity.diary.DiaryTimelineActivity" />
    </application>

</manifest>